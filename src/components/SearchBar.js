import React, {Component} from 'react';

class SearchBar extends Component {
    render() {
        return (
            <div>
                search bar
            </div>
        );
    }
}

export default SearchBar;